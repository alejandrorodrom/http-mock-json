export const MOCK_CONFIG_FILENAME = 'mock.config.json';

export const MOCK_CONFIG_FOLDERS_PROPERTY = 'folders';

/** Default listen port when CLI `-p` / `--port` and `mock.config.json` `port` are omitted. */
export const DEFAULT_MOCK_PORT = 3001;
