export const RECORDINGS_DIR = '.recordings';

export const RECORDINGS_FILES_DIR = 'files';

export const RECORDINGS_DIR_PATTERN = /(^|[\\/])\.recordings([\\/]|$)/;

export type RecordingsMode = 'include' | 'exclude' | 'only';
