openapi: 3.0.3
info:
  title: Broken YAML
  version: 1.0.0
paths:
  /ping:
    get:
      responses:
        '200':
          description: OK
          content:
            application/json:
              example:
                ok: true
  this is : [ invalid: yaml: {{{
